  # ── CRM_CE ────────────────────────────────────────────────────

  - name: fact_meeting
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/fact_meeting.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_meeting_bronze, alias: fact_meeting}

  - name: fact_client_ptcpt
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/fact_client_ptcpt.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_meeting_contact_part_bronze, alias: fact_client_ptcpt}

  - name: dim_sub_type
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/dim_sub_type.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_meeting_sub_type_bronze, alias: dim_sub_type}

  - name: dim_body_notes
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/dim_body_notes.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_body_notes_bronze, alias: dim_body_notes}

  - name: fact_event
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/fact_event.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_event_bronze, alias: fact_event}
        - {table: polaris.alpha.compass.crm_plus_currency_exchange, alias: currency_exchange}

  - name: dim_sub_event
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/dim_sub_event.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_subevent_bronze, alias: dim_sub_event}

  - name: fact_sub_event_attendees
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/fact_sub_event_attendees.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_event_attendee_bronze, alias: fact_sub_event_attendees}

  - name: fact_bnpp_ptcpt
    layer: beta
    sub_layer: trusted
    engine: spark
    write_mode: overwrite
    metadata: {domain: crm_ce}
    spark_config: {additional_configs: {spark.sql.ansi.enabled: "false"}}
    data_quality: {on_failure: fail, checks: [{type: row_count_min, value: 1}]}
    transform:
      sql_file: sql/crm_ce/fact_bnpp_ptcpt.sql
      iceberg_properties: {format-version: "3"}
      sources:
        - {table: polaris.alpha.crm_ce.crm_ce_meeting_bnpp_part_bronze, alias: fact_bnpp_ptcpt}
        - {table: polaris.beta.trusted.crm_plus.dim_emp_info, alias: dim_emp_info}
